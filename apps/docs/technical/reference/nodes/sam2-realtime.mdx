---
title: "Segment Anything 2"
description: "This node is useful for tracking an object given positive and negative prompt coordinates. It can be combined with Florence2 and other object detection nodes."
icon: "code"
---

1. Navigate to the `custom_nodes` directory in your ComfyUI workspace
<Tabs>
    <Tab title="RunPod (Linux)">
    ```bash
    cd /workspace/comfyRealtime/ComfyUI/custom_nodes
    ```
    </Tab>

    <Tab title="Local (Windows)">
    ```bash
    cd "$env:HOMEPATH/comfyRealtime/ComfyUI/custom_nodes"
    ```
    </Tab>
</Tabs>

2. Clone the repository
```bash
git clone https://github.com/pschroedl/ComfyUI-SAM2-Realtime.git
cd ComfyUI-SAM2-Realtime
```

3. Install requirements
<Tabs>
    <Tab title="RunPod (Linux)">
    ```bash
    conda activate comfyui
    pip install -r requirements.txt
    ```
    </Tab>

    <Tab title="Local (Windows)">
        <Info>Ensure the Windows prerequisites are installed first, refer to the Windows section in [Install Nodes in ComfyUI](../../reference/custom-node-installation##install-nodes-in-comfyui)</Info> 
        ```bash
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
        pip install -r requirements.txt
        pip install . --no-build-isolation
        ```
    </Tab>
</Tabs>

4. Download sample workflow for Segment Anyting 2 [examples/SAM2-Realtime-Example.json](https://github.com/pschroedl/ComfyUI-SAM2-Realtime/blob/main/examples/SAM2-Realtime-Example.json) and open it in ComfyUI

5. Proceed to [Install Nodes in ComfyStream](../../reference/custom-node-installation#install-nodes-for-comfystream) each time you add a new node to ComfyUI